{% extends 'layout/base.twig' %}

{% block body %}
    <div id="app"></div>

    <script type="application/javascript">
        window.__CONFIG__ = {
            csrf: "{{ csrf_token('unique_string') }}"
        };
    </script>
{% endblock %}
